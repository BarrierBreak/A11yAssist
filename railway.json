{
  "services": {
    "a11yassist-backend": {
      "runtime": "python",
      "plan": "starter",
      "build": {
        "commands": [
          "pip install -r requirements.txt"
        ]
      },
      "start": "gunicorn --bind 0.0.0.0:$PORT -k gevent --worker-connections 1000 --timeout 0 app:app",
      "environment": {
        "BarrierBreak": "$KEY",
        "RENDER": "true"
      }
    }
  }
}
